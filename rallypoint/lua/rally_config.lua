rallycfg = {}

// in-game ranks that can access the menu and create spawn points
rallycfg.Ranks = {
	["superadmin"] = true,
}

// job/team categories that can access the menu and create spawn points
rallycfg.Categories = {
	["Combat Engineers"] = true,
	["Fleet"] = true,
}

